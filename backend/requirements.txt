# =============================================================================
# DigiID — Dépendances backend Python
# Python 3.11+
# =============================================================================

# --- Framework web et serveur ---
fastapi==0.110.0
uvicorn[standard]==0.27.1
gunicorn==21.2.0

# --- Validation et configuration ---
pydantic==2.6.1
pydantic-settings==2.2.1
email-validator==2.1.0

# --- Base de données ---
sqlalchemy==2.0.27
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary

# --- Sécurité et authentification ---
argon2-cffi==23.1.0
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
python-multipart==0.0.9
cryptography==42.0.4
pyotp==2.9.0
qrcode[pil]==7.4.2

# --- Cache et tâches asynchrones ---
# redis==5.0.1        # Désactivé pour Render Free (pas de Redis)
# celery==5.3.6       # Désactivé pour Render Free (nécessite Redis)

# --- Limitation de débit ---
slowapi==0.1.9  # Importé dans src/main.py

# --- Journalisation et monitoring ---
loguru==0.7.2
# prometheus-fastapi-instrumentator==6.1.0  # Allégé pour Render Free
sentry-sdk[fastapi]==1.40.5

# --- LLM et chatbot (RAG avec LangChain) ---
# ⚠️ sentence-transformers désactivé pour Render Free (trop lourd : ~2 Go RAM)
langchain==0.1.9
langchain-community==0.0.24
langchain-openai==0.0.7
chromadb==0.4.22
# sentence-transformers==2.5.1  # Désactivé pour Render Free
tiktoken==0.6.0

# --- Machine Learning (scoring) ---
# scikit-learn==1.4.0     # Allégé pour Render Free
# xgboost==2.0.3          # Allégé pour Render Free
# shap==0.45.0            # Allégé pour Render Free
pandas==2.2.0
numpy==1.24.3
faker==23.2.1

# --- Deep Learning (vérification visuelle + recherche faciale) ---
opencv-python-headless==4.9.0.80
deepface==0.0.79
tensorflow==2.15.0
# insightface==0.7.3           # Alternative plus légère (optionnel)
# onnxruntime==1.17.1          # Nécessaire pour insightface
pillow==10.2.0

# --- OCR (CNI, documents, cartes d'identité) ---
pytesseract==0.3.10  # OCR Tesseract (nécessite tesseract-ocr système)

# --- Utilitaires ---
python-dateutil==2.8.2
pytz==2024.1
httpx==0.27.0
tenacity==8.2.3

# --- Tests ---
pytest==8.0.2
pytest-asyncio==0.23.5
pytest-cov==4.1.0
factory-boy==3.3.0
